#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" && pwd)"
CLI="${ROOT}/.venv/bin/robohermes-libero"
if [[ ! -x "${CLI}" ]]; then
  printf 'RoboHermes is not installed. Run ./setup.sh first.\n' >&2
  exit 2
fi
exec "${CLI}" "$@"
